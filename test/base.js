var expect    = require("chai").expect;
var request = require("request");

describe("Base http server", function(){
    it("Adds numbers correctly", function() {
        expect(1+1).to.equal(2);
    });
});